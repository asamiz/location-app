import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  container: {
    marginTop: 20,
    backgroundColor: 'red',
  },
  dropDownContainer: {
    height: 40,
    marginBottom: 25,
  },
  dropDownItem: {
    justifyContent: 'flex-start',
  },
  title: {
    fontSize: 18,
    marginBottom: 8,
  },
});
